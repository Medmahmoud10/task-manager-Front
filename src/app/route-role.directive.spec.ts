import { RouteRoleDirective } from './route-role.directive';

describe('RouteRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteRoleDirective();
    expect(directive).toBeTruthy();
  });
});
